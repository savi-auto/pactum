[project]
name = "pactum"
description = "Trustless escrow platform for the Bitcoin economy on Stacks"
authors = []
telemetry = false
cache_dir = "./.cache"

# Trait must be deployed first
[contracts.escrow-storage-trait]
path = "contracts/escrow-storage-trait.clar"
clarity_version = 3
epoch = 3.0

# Storage contract (data layer)
[contracts.escrow-storage]
path = "contracts/escrow-storage.clar"
clarity_version = 3
epoch = 3.0

# Logic contract (business rules)
[contracts.escrow-logic]
path = "contracts/escrow-logic.clar"
clarity_version = 3
epoch = 3.0
